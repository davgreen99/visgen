flask>=3.0
flask-cors>=4.0
librosa>=0.10
numpy>=1.24
soundfile>=0.12
torch>=2.0
pandas>=2.0
scikit-learn>=1.3
imageio-ffmpeg>=0.4
gunicorn>=21.0           # production WSGI server (used by the Dockerfile / deployed backend)

# --- Optional: CLAP, for zero-shot labelling (server/clap_embeddings.py) ---
# Deliberately not installed here: it downloads large model weights and nothing at inference
# time needs it. It also requires its own Python 3.11 environment: laion-clap pins numpy
# below 2, and that has no wheel for Python 3.13.
#   pip install laion-clap torchvision
